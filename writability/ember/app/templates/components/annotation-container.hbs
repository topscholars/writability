{{#each annotationGroup in existingAnnotationGroups}}
	{{annotation-groupcontainer group=annotationGroup isStudent=isStudent deleteAnnotation_GroupContainer="deleteAnnotation_Container" }}
{{/each}}

{{#if newAnnotation}}
	{{annotation-createbox annotation=newAnnotation tags=tags hasSavedAnnotation="hasSavedAnnotation"}}
{{/if}}
